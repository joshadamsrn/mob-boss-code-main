# Codex Project Context (mob-boss-code)

Last updated: 2026-03-27
Primary repo path: `/Users/Adams/Desktop/mob-boss-code-main`

## Session Startup Checklist
1. `cd ~/Desktop/mob-boss-code-main`
2. Run `./scripts/load-context.sh`
3. Confirm working tree: `git status --short`
4. If developing locally, start services as needed:
   - Django: `python3 project/mobboss_apps/manage.py runserver`
   - Frontend assets (optional): `npm run dev`
5. Run targeted verification for changed area before ending session.

## Project Overview
- Stack: Django + modular monolith (ports/adapters, hexagonal architecture).
- Django root: `project/mobboss_apps/`
- Manage entrypoint: `project/mobboss_apps/manage.py`
- Main gameplay web page: `/games/<game_id>/`
- Lobby web page: `/`

## Current Goals
- Continue iterating gameplay UX and economy/trial rules in dev mode.
- Keep room lifecycle and gameplay lifecycle synchronized.
- Preserve role/privacy constraints in player vs moderator views.

## Latest Pickup (2026-03-27)
- Phone player layout is now the default non-moderator flow for merchants too.
- Merchant view split is complete:
  - wallet/game card renamed to `Money and Central Supply`,
  - merchant inventory now lives on the shared inventory screen,
  - merchants can still sell items back to central supply from inventory.
- Endgame phone flow is updated:
  - winner overlay routes to participants/ghost-style screen after `Okay` or timeout,
  - `Exit Game` is available on the participants/notepad view,
  - endgame lockout now preserves only the winner `Okay` button and both `Exit Game` buttons.
- Notification system changed significantly:
  - notification history card added below `Phase Order`,
  - dismissed notifications are persisted in session storage,
  - blocked overlays no longer consume pending notifications,
  - overlay notices are now only marked seen after player dismiss or full timeout,
  - phone ding plays for new overlay notices and incoming offers after audio unlock.
- Polling is now `3s` by default.
  - `2s` caused excessive churn; `3s` is the current tuned value.
  - initial phone snapshot includes `phase`, `round_number`, and `version` to avoid false reload flashes.
- Inventory action UX on phone was simplified:
  - resale, sell-offer, and gift-item controls are now single-submit,
  - no two-click toggle requirement remains in player inventory.
- Trial/superpower notice updates completed:
  - Don silence target gets direct trial-silence notice,
  - Captain freeze, Deputy custody, and Sergeant capture/release use direct player-facing notices,
  - accused player now receives: `You have been accused of murdering <victim>. You are being placed on trial.`
- Dev/view-as HTML action fixes:
  - Under Boss, Kingpin, and Gangster trial powers now resolve the impersonated actor correctly in dev mode.
- Under Boss/Gangster/Kingpin and phone notification regressions are covered in tests.
- Current deployment state:
  - commit `f052f08` pushed to `origin/main`,
  - server `134.199.226.15` pulled and deployed that commit,
  - `mobboss.service` is active and listening on `127.0.0.1:8000`,
  - both `curl -I http://127.0.0.1:8000/` and `curl -I http://134.199.226.15/` return `302` to `/auth/?next=/`.

## Important Commands
- Run Django checks:
  - `python3 project/mobboss_apps/manage.py check`
- Run key gameplay suites:
  - `python3 -m unittest project.mobboss_apps.gameplay.tests.domain.test_game_service`
  - `python3 project/mobboss_apps/manage.py test project.mobboss_apps.gameplay.tests.rest.test_gameplay_v1_views project.mobboss_apps.gameplay.tests.rest.test_gameplay_views_context`
  - `python3 -m unittest project.mobboss_apps.gameplay.tests.adapters.test_page_view_mapper`
- Start local server:
  - `python3 project/mobboss_apps/manage.py runserver`
- Build/watch frontend assets:
  - `npm run dev`
  - `npm run build`
- Dev lifecycle cleanup utility:
  - Dry run: `python3 project/mobboss_apps/manage.py clear_stale_lifecycle`
  - Apply: `python3 project/mobboss_apps/manage.py clear_stale_lifecycle --apply`

## Environment and Setup Notes
- Run commands from repo root: `~/Desktop/mob-boss-code-main`.
- Python: use `python3`.
- DB path behavior is intentionally absolute and stable across launch directories.
  - Set in: `project/mobboss_apps/mobboss/adapters/outbound/credentials_memory_impl.py`
  - Relative DB names are normalized to repo-root DB file.

## File Locations (high signal)
- Gameplay domain logic: `project/mobboss_apps/gameplay/src/game_service.py`
- Gameplay HTML controller: `project/mobboss_apps/gameplay/views.py`
- Gameplay JSON API controller: `project/mobboss_apps/gameplay/v1_views.py`
- Gameplay templates: `project/mobboss_apps/gameplay/templates/gameplay/`
- Gameplay DTO/contracts:
  - `project/mobboss_apps/gameplay/ports/internal.py`
  - `project/mobboss_apps/gameplay/ports/internal_requests_dto.py`
  - `project/mobboss_apps/gameplay/ports/inbound.py`
- Composition root: `project/mobboss_apps/mobboss/composition.py`
- Rooms service: `project/mobboss_apps/rooms/src/room_service.py`
- Operations command (stale cleanup):
  - `project/mobboss_apps/operations/management/commands/clear_stale_lifecycle.py`

## Known Constraints and Conventions
- Follow `CONVENTIONS.md` (ports/adapters, DTO parsing via `from_payload`, domain logic in `src/`, Django at edges).
- `rooms` owns pre-game/lobby; `gameplay` owns in-round lifecycle/trial/economy behavior.
- Prefer targeted tests for edited area before broad runs.
- Keep player-role leakage blocked in non-moderator projections.

## Recent Changes (high-level)
- 2026-03-27 gameplay/phone update bundle:
  - merchant phone UX split finalized,
  - endgame participants routing fixed,
  - phone notification overlay persistence/dedupe/timing hardened,
  - notification history added,
  - phone action ding expanded,
  - refresh polling tuned from 5s -> 2s -> 3s final,
  - inventory actions changed to single-submit,
  - accused-on-trial direct notice added,
  - dev-mode actor resolution fixed for Under Boss / Kingpin / Gangster HTML actions.
- Room/game lifecycle sync and stale cleanup command added.
- Jury flow improved:
  - juror notice shown,
  - vote buttons gated behind moderator start,
  - juror countdown added,
  - countdown hidden after vote.
- Murder self-report added:
  - player can report own murder and select murderer,
  - murdered inventory transfers to murderer immediately.
- Trial outcome updates:
  - guilty/not guilty notice broadcasting,
  - guilty -> jailed,
  - jailed inventory transfer based on conviction correctness:
    - correct accusation -> Police Chief receives inventory,
    - incorrect accusation -> Mob Boss receives inventory.
- Merchant UX/economy updates:
  - merchant card consolidated into one section,
  - money balance + money goal shown,
  - gifting integrated into merchant card,
  - sale offers require buyer accept/decline prompt,
  - insufficient-funds on accept only notifies buyer; merchant sees decline,
  - merchant can sell item back to central supply for 90% of acquisition.
- Participant badge update:
  - accused during `trial_voting` now displays `On Trial`.
- Gameplay ledger hardening completed for currently implemented routes:
  - ledger entry model added to gameplay snapshots and sqlite persistence,
  - ledger entries recorded for supply purchases, supply buybacks, accepted money gifts,
    murder transfers, and conviction transfers,
  - strict reconciliation re-enabled so money cannot silently leave the game in implemented flows,
  - death flow preserves murdered-player resources on the dead participant until a real recipient exists.
- Phase B completed:
  - EFJ hook points added to guilty-verdict resolution,
  - EFJ item auto-consumes on guilty verdict,
  - accused remains active after EFJ auto-use,
  - EFJ purchase value transfers from the central-supply ledger holder to a police recipient,
  - recipient only gets a private bribe notification,
  - no public recipient leak.
- Phase C completed:
  - vest-blocked gunshots now consume the vest,
  - no trial starts when a vest blocks a reported gunshot,
  - vest value transfers from `central_supply` to the attacker to preserve reconciliation,
  - attacker identity stays hidden from the public vest notice,
  - murder reports now require `attack_classification`,
  - public murder notice includes weapon display name.
- Role-ability UI baseline completed:
  - alive participants now always get a role-ability card,
  - `Don`, `Under Boss`, and `Kingpin` keep actionable controls,
  - passive/automatic roles now show specific “current hooks” details,
  - placeholder roles now show role-specific summary/detail blocks instead of generic filler.
- Deputy activated power implemented:
  - `Protective Custody` (once/game, information phase),
  - target alive/non-self, 5-minute immunity window,
  - attempted murder still starts trial but target remains alive with no transfer,
  - timer and notices visible to moderator + deputy + target.
- Sheriff activated power implemented:
  - `View Jury Log` (twice/game, any in-progress phase),
  - reveals most recent jury roster only (no votes),
  - fails with `No jury history yet` before first jury,
  - sheriff reveal lasts 60 seconds; moderator has persistent latest-jury visibility.
- Captain activated power implemented:
  - `Asset Freeze` (once/game, any in-progress phase),
  - freezes one alive non-self target for 10 minutes,
  - blocks money actions (buy/sell/gift send/receive),
  - cancels pending sale/gift/money offers involving target and notifies involved users,
  - timer visible to moderator + captain + target.
- Lieutenant activated power implemented:
  - `Information Briefcase` (once/game, any in-progress phase),
  - privately reveals alive faction counts (Police/Mob/Merchant) to Lieutenant,
  - reveal lasts 60 seconds with no countdown.
- Sergeant activated power implemented:
  - `Capture` (once/game, information phase only),
  - one active captured player at a time (alive non-self target),
  - captured player is blocked from actions/powers and cannot be murder target while captured,
  - timer visible to moderator + sergeant + captured player,
  - auto-release branch if required leader responsibilities cannot be reassigned.
- Detective activated power implemented:
  - `Investigation` (once/game, any in-progress phase),
  - can target any participant, including alive, dead, or jailed players,
  - reveal visible to Detective only for 60 seconds with no countdown,
  - shows the target's last 3 player-to-player transactions only,
  - historical sale + money-gift events backfill from ledger where available,
  - accepted sales, money gifts, and item gifts now persist as player transaction history.
- Inspector activated power implemented:
  - `Record Inspection` (once/game, any in-progress phase),
  - can target one dead or jailed participant except self,
  - reveal visible to Inspector only for 60 seconds with no countdown,
  - shows only the target's role name,
  - fails with `No jail or morgue records available yet.` when no eligible targets exist.
- Police Officer activated power implemented:
  - `Confiscation` (once/game),
  - can be armed during `information`, `accused_selection`, or active `trial_voting` before all votes are submitted,
  - applies to the next guilty verdict only,
  - if the officer is still alive and EFJ does not trigger, jailed inventory is liquidated to cash and redistributed between the Police Officer and other alive police,
  - if the officer is inactive, EFJ triggers, the verdict is not guilty, or no recoverable resources exist, the armed effect is still consumed and resolved via notices,
  - normal single-recipient conviction inheritance is overridden when confiscation succeeds.
- Cop automatic power implemented:
  - auto-protects once/game when alive non-jailed player count reaches 3 or fewer,
  - system-generated bulletproof vest is added if Cop does not already have one,
  - if Cop already has a vest, the trigger is still consumed and only notices fire,
  - only Cop and moderator are notified,
  - status-only card shows `Active` / `Inactive`.
- Enforcer automatic power implemented:
  - first successful reported murder grants a one-time 50% cash bonus based on victim transferred money only,
  - bonus rounds up to the next `$10`,
  - bonus is system-generated and ledgered from `central_supply`,
  - zero-cash kill still consumes the power,
  - only Enforcer and moderator are notified,
  - status-only card shows `Active` / `Used`.
- Made Man activated power implemented:
  - `Skip Middle Man` (once/game),
  - may buy one currently active central-supply item at the live supply price during any `in_progress` phase,
  - still pays with own money and remains blocked by freeze/capture/death/jail rules,
  - insufficient funds do not consume the power,
  - moderator + active merchant-role players + Made Man are notified with burglary flavor text / confirmation,
  - actionable role card is live.
- Merchant activated power implemented:
  - `Wholesale Order` (once/game, information phase only),
  - only the base `Merchant` role gets this power,
  - buys one active central-supply item at a 30% discount from the listed price,
  - discounted price uses the normal nearest-$10 rounding,
  - purchase still consumes shared stock and is ledgered from player to `central_supply`,
  - insufficient funds do not consume the power.
- Arms Dealer automatic power implemented:
  - `Starting Gun Cache` (automatic at game launch),
  - Arms Dealer starts with one Tier 1 gun in inventory,
  - that gun is removed from shared central supply stock at session start,
  - only the Arms Dealer receives the startup notification,
  - no manual activation card action is required.
- Smuggler activated power implemented:
  - `Smuggle` (once/game, information phase only),
  - targets one alive non-self player who is not currently captured,
  - steals one truly random eligible item from the target's current inventory on the server,
  - items already tied up in pending sale/gift offers are ineligible,
  - if no eligible item exists, the power is still consumed,
  - private result notices go to Smuggler, target, and moderator only,
  - successful theft preserves acquisition value/resale price and is stored in Detective-visible transaction history as `Stolen Item`.
- Gun Runner activated power implemented:
  - `Charisma` (once/game, information phase only),
  - activation starts a 3-minute timer visible on the Gun Runner role card,
  - every accepted player-to-player sale during the active window pays an extra 30% system bonus based on the accepted sale price,
  - bonus rounds to the normal nearest-$10 rule,
  - bonus is generated from `central_supply` and does not charge the buyer extra,
  - only Gun Runner and moderator are notified on activation and each payout,
  - frozen accounts do not block activation, but still block sale completion while the timer runs,
  - captured state blocks activation.
- Central supply stock behavior changed:
  - active catalog items are now shared one-time stock globally,
  - when a supply item is bought, it becomes unavailable to everyone,
  - when a merchant sells an item back to central supply, that catalog item becomes active again and is available for purchase,
  - Made Man still cannot sell to supply because sell-back remains merchant-only.
- Gangster activated power implemented:
  - `Tamper` (once/game, one trial only),
  - available during active `trial_voting` after jury selection and before effective vote completion,
  - targets one current juror; the juror remains visibly on the jury but their counted vote is suppressed for that trial,
  - Gangster gains a separate replacement vote slot with its own fresh 10-second timer,
  - if Gangster is already a juror, Gangster may cast both the normal jury vote and the tamper vote,
  - if the targeted juror already voted, that counted vote is removed immediately,
  - if Gangster dies, is jailed, or is captured before resolution, tamper cancels and the juror's vote counts normally again,
  - silencing does not cancel tamper,
  - only Gangster, targeted juror, and moderator are notified; moderator trial UI also shows the tampered juror.
- Street Thug activated power implemented:
  - `Steal` (once/game),
  - available during any in-progress phase,
  - targets one alive non-self participant who is not currently captured,
  - if target has at least `$100`, exactly `$100` moves from target to Street Thug,
  - if target has less than `$100`, no money moves and the power is still consumed,
  - Street Thug, target, and moderator are notified of the result,
  - successful steals are persisted in the gameplay ledger for reconciliation.
- Ghost View implemented:
  - dead or jailed players get a `Ghost View` banner and moderator-style information visibility,
  - ghost viewers are warned not to speak to active players or show their device,
  - ghost view is informational only and does not expose moderator mutation controls.
- Felon automatic power implemented:
  - `Escape` (automatic on jail),
  - when a `Felon` is jailed, a 30-minute sit-out timer starts,
  - Felon and moderator are notified that the Felon must sit out and cannot talk to active players or view their devices,
  - while the timer is active, the jailed Felon does not receive Ghost View,
  - if the game is still in progress when the timer expires, the Felon returns from `jailed` to `alive`,
  - re-entering Felon balance is reset to `$10`,
  - public notice announces that a felon escaped and is back in the game.
- Trial-voting timeout behavior clarified/implemented:
  - when vote clocks expire, the trial resolves using only counted votes submitted before expiry,
  - missing juror votes count as no vote,
  - guilty requires strictly more counted `guilty` votes than counted `innocent` votes,
  - ties resolve as `innocent` / not guilty.
- UI/control polish updates completed on 2026-03-22:
  - Role Ability cards now render character portraits from static assets at `project/mobboss_apps/static/characters/` using per-role image mapping in `project/mobboss_apps/gameplay/views.py`.
  - Role Ability card layout updated so portrait is left, all card text/actions stay in the right column, and portrait sizing is constrained for cleaner alignment.
  - Added a large `Character Role` header card under Dev View Tabs for character screens; moderator screen now also shows a prominent `Moderator` role header.
  - Mob Boss role-card copy was customized and Mob Boss now uses a top-position banner message (same placement pattern as other role cards).
  - `Police Mob Kills : X / Allowed: Y` tracker is moderator/ghost-view only in both template rendering and v1 JSON payload projection.
  - `Advance Accused Timeout` moved from in-game moderator card to `/options/` (under `Kill Game`) and remains moderator-only.
  - `Moderator Controls` report-death card moved from gameplay page to `/options/` and renamed `Moderator Report Death` with same core murder report inputs.
  - Added `Return to Game` button on `/options/` when an active game is available.
  - Player self-report murder card now defaults `Attack Type` to `Select Murder Weapon` (no preselected knife).
- Mobile-first UX pass completed after 2026-03-22 context snapshot:
  - user explicitly set phone as the primary target: if there is a formatting tradeoff, prefer what looks best on a phone.
  - lobby `New Room` modal was removed because room creation was freezing on phone/desktop; room creation is now an inline form on `/`.
  - shared shell moved toward an app feel:
    - tighter mobile top bar,
    - safe-area-aware spacing,
    - full-width mobile buttons and stacked form rows.
  - room/game/merchant/wallet layouts were updated to stack better on narrow screens and avoid cramped side-by-side controls.
  - role portrait was removed from `project/mobboss_apps/gameplay/templates/gameplay/_superpower_card.html`.
  - role portrait now lives in the `Character Role` card in `project/mobboss_apps/gameplay/templates/gameplay/detail.html`, centered below the role name.
  - the current working portrait sizing is enforced inline in `detail.html` to beat stale cached CSS on phones:
    - width `132px`,
    - height `auto`,
    - proportional scaling,
    - placement is correct but may still need final visual tuning.
  - current visual rule from user: portrait should stay in the `Character Role` card, centered, and sized to fit cleanly on a phone screen.
- Lifecycle/stale cleanup updates completed on 2026-03-24:
  - `clear_stale_lifecycle` was run and stale gameplay sessions were ended.
  - a specifically stuck room (`Sam's room`) was force-ended via `kill_game`.
  - gameplay now auto-ends after 24 hours of inactivity (`last_progressed_at_epoch_seconds` persisted in snapshots/payloads),
  - rooms list now refreshes linked gameplay lifecycle and drops auto-ended stale in-progress rooms.
- Room launch/shuffle behavior updates completed on 2026-03-24:
  - continuous lobby auto-shuffle was removed from the room page,
  - the `Shuffle` button was removed from the members header,
  - role/faction/rank shuffle now happens one time at moderator launch.
- Central supply default icon replacement completed on 2026-03-24:
  - new assets loaded from `central-supply-item-icons/` and mapped to default catalog slots,
  - defaults now use direct `.jpg/.png` paths (instead of `.svg` wrappers),
  - existing `room_items.image_path` rows were migrated from old `.svg` defaults to new `.jpg/.png` defaults.
- Mobile shell navigation updates completed on 2026-03-24:
  - bottom mobile nav shortcuts were removed to prevent overlap with input fields,
  - `Lobby`/`Room`/`Game`/`Options` were moved into the top-right profile dropdown,
  - mobile browser chrome blending improved with dark `theme-color` meta tags and explicit dark `html` background.

## Next Actions
- Continue role-by-role superpower implementation after `Gun Runner`.
- Next planned role: `Supplier`.
- Merchant-faction clarification:
  - canonical merchant-type faction remains `Merchant`,
  - merchant-type roles are `Merchant`, `Arms Dealer`, `Smuggler`, `Gun Runner`, and `Supplier`,
  - each is a solo merchant-faction player with an individual money-goal win condition,
  - merchant-type players otherwise participate normally: can murder, serve on juries, be jailed, and be killed,
  - at 7 players, the inserted merchant-type role is always `Merchant`,
  - additional merchant-type roles are added only when player count requires more merchant slots, and those extra roles are chosen randomly from the non-base merchant-role set.
- For each next role:
  - define authoritative trigger/limits/targets/visibility/state impact,
  - implement full vertical slice (domain + DTO/endpoint + view/template + tests),
  - update planning docs/matrix/checklist after behavior is live.
- Revisit deferred 409 conflict-retry UX only after role-ability rules/UI stabilize.
- If continuing UI work before `Supplier`, prioritize phone-first cleanup over desktop polish.
- Playtest end-to-end moderator + phone client flow for:
  - sale offer send/accept/decline,
  - insufficient-funds accept case,
  - self-reported murder transfer,
  - on-trial badge transitions,
  - EFJ guilty-verdict flow,
  - vest-blocked gunshot flow,
  - required attack-type murder reporting,
  - deputy custody flow,
  - sheriff jury-log reveal window,
  - captain asset-freeze cancellation/blocks,
  - lieutenant briefcase reveal window,
  - sergeant capture flow and auto-release branch.
  - detective transaction-history privacy + room/game isolation,
  - smuggler burglary success / no-item consumption / detective stolen-item visibility,
  - gun-runner charisma activation while frozen / payout while active / no payout after expiry,
  - inspector dead/jailed role reveal window,
  - police-officer confiscation success/cancel/EFJ branches.
  - cop auto-vest trigger on death path + guilty-verdict path,
  - enforcer first-kill bonus payout / zero-cash consumption,
  - made-man direct central-supply purchase and merchant stock reactivation after buyback,
  - gangster tamper early vote removal / double-vote flow / cancellation path / timeout path.
  - lobby room creation from phone after modal removal.
  - character-role portrait sizing/placement on real phone screens and browser cache behavior.
  - top-right profile dropdown nav (`Lobby` / `Room` / `Game` / `Options`) and typing usability without bottom-nav overlap.
  - central supply item thumbnails rendering correctly from default `.jpg/.png` paths on real phone browsers.

## Notes for Future Codex Sessions
- Before editing, load this file and scan `CONVENTIONS.md`.
- Keep changes minimal and scoped; avoid cross-domain coupling.
- After changes, run at least one domain test suite + one view/API suite.
- If behavior depends on phase/status, verify both `in_progress` and boundary/ended cases.
- For UX/layout work, treat phone as the primary surface, not desktop.
- Be careful with cached CSS on phone. For critical image sizing, inline dimensions may be necessary until bundles are rebuilt/reloaded.
- Current recommended execution order:
  1. Quick phone validation pass for latest nav/icon changes.
  2. Then start the `Supplier` role power slice.
  3. Playtest the merchant-role powers (`Wholesale Order`, `Starting Gun Cache`, `Smuggle`, `Charisma`) in moderator + phone flow.
  4. Playtest trial timeout + tamper edge cases in moderator + phone flow.
  5. Revisit deferred 409 auto-retry UX only after role ability behavior stabilizes.

## Exact Pickup Point
- Do not start with 409 retry UX.
- If continuing role-power implementation, start in `project/mobboss_apps/gameplay/src/game_service.py`, `project/mobboss_apps/gameplay/views.py`, and `project/mobboss_apps/gameplay/templates/gameplay/_superpower_card.html` for the `Supplier` full slice.
- If continuing recent UX/control work, primary files are:
  - `project/mobboss_apps/web/webpack/styles.css`
  - `project/mobboss_apps/web/templates/web/base.html`
  - `project/mobboss_apps/web/templates/web/lobby.html`
  - `project/mobboss_apps/gameplay/templates/gameplay/_superpower_card.html`
  - `project/mobboss_apps/gameplay/templates/gameplay/_player_report_murdered_card.html`
  - `project/mobboss_apps/gameplay/templates/gameplay/detail.html`
  - `project/mobboss_apps/web/templates/web/options.html`
  - `project/mobboss_apps/web/views.py`
  - `project/mobboss_apps/web/urls.py`
- Existing role-ability state:
  - live activated cards: `Don`, `Under Boss`, `Kingpin`, `Deputy`, `Sheriff`, `Captain`, `Lieutenant`, `Sergeant`, `Detective`, `Inspector`, `Police Officer`, `Made Man`, `Gangster`, `Street Thug`, `Merchant`, `Smuggler`, `Gun Runner`
  - live passive/automatic cards: `Chief of Police`, `Mob Boss`, `Knife Hobo`, `Cop`, `Enforcer`, `Arms Dealer`
  - remaining unimplemented merchant-type role power is `Supplier`
- The next concrete deliverable is the `Supplier` role power full slice, with `project/mobboss_apps/gameplay/src/game_service.py`, `project/mobboss_apps/gameplay/views.py`, and `project/mobboss_apps/gameplay/templates/gameplay/_superpower_card.html` as the likely entry points.
- The immediate UX pickup point, if the user continues phone polish, is validating the latest `base.html` dropdown-nav move and `styles.css` mobile chrome/background behavior on real phones, then tuning spacing if needed.

## Session Update 2026-03-25 (Phone-first UX + Gameplay Rules)
- Profile dropdown cleanup completed:
  - removed `Room` and `Game` items from top-right profile menu (`base.html`).
- Notes chip layout fixed for phone:
  - standardized to a consistent 2x2 grid with aligned button sizing (`_participants_card.html`, `styles.css`, plus cache-safe inline override in `detail.html`).
- Merchant balance/goal alignment corrected in merchant inventory card for mobile (`_merchant_inventory_card.html`, `detail.html`).
- Shot grammar fixed:
  - central supply gun labels now render `1 shot` (singular) instead of `1 shots` (`rooms/detail.html` + defensive display normalization in `gameplay/views.py`).
- Role Ability card alignment normalized to left alignment to match other cards (`styles.css` + inline override in `detail.html`).
- Action prompt UX added:
  - temporary actionable prompts now surface at top of gameplay with jump links,
  - ding sound added when new actionable prompts appear,
  - polling now continues when tab is hidden (`detail.html` + card IDs across gameplay partials).
- Murder-report weapon selector simplified:
  - only `Knife` or `Gun` shown in dropdowns; no gun tier display,
  - attacker placeholder changed to `Select attacker`.
- Inventory icon UX restored:
  - small item icon appears left of item description in inventory rows (`_player_wallet_card.html`, `_merchant_inventory_card.html`).
- Merchant inventory interaction refactors completed:
  - `Set Price` toggles to editable mode and transforms to `Save Price`, then collapses on save.
  - `Sell Item` reveals buyer selector and transforms to `Send Offer`, then collapses on send.
  - `Gift Item` reveals player selector and transforms to `Send Gift`, then collapses on send.
  - same Gift toggle pattern applied to non-merchant player inventory.
- Offer response layout updated:
  - Accept/Decline buttons now render side-by-side and centered (including mobile).
- Price information visibility rule updated:
  - non-merchant players now only see `Purchase Price: $X`,
  - merchant-role views retain acquisition + resale details.
- Knife Hobo starting knife image source aligned to central-supply knife defaults:
  - now resolves from `knife`/`knife_*` catalog entries for consistent icon display (`game_service.py`).
- Sell-back economics changed:
  - merchant sell-back to central supply now returns 100% of acquisition value (was 90%),
  - button label updated to `Sell Back to Central Supply`,
  - sell-back button sizing matched to other card buttons.
- Jury secrecy fix:
  - `Most Recent Jury (Moderator)` notice now restricted to moderator projection only (not regular players / ghost projections).
- Dev tabs status colors updated:
  - active dead/jailed ghost-view tabs stay grey,
  - inactive dead/jailed ghost-view tabs are red.
- Don Intimidation full behavior update:
  - Don can arm Intimidation during information phase,
  - on next reported murder, armed target is auto-silenced for upcoming trial,
  - all players + moderator receive required notification text about silence,
  - armed state is consumed after application,
  - fixed activation actor resolution bug that caused `participant not found in session` in dev/view-as contexts.
- Murder broadcast banner update:
  - now red and formatted as:
    - `[PLAYER] WAS MURDERED WITH [GUN|KNIFE] - REPORT IMMEDIATELY TO COURT HOUSE`
  - weapon type is generic (`GUN` or `KNIFE`), no tier/weapon specificity.
- Endgame lockout behavior implemented:
  - when game is ended, gameplay controls become view-only,
  - only `Exit Game And Close Room` remains actionable,
  - exit button styled bright red and normalized to card button sizing.
- Parse error fix for ended-state template:
  - resolved malformed template conditional that caused:
    - `Could not parse some characters: page.status|ended|yesno:'true,false'`
  - replaced with explicit `{% if page.status == "ended" %}...{% endif %}` boolean output in `detail.html`.

## Validation Snapshot (2026-03-25)
- `python3 project/mobboss_apps/manage.py check` passing after iterative changes.
- Targeted tests passing:
  - `GameplayServiceTests.test_start_session_gives_knife_hobo_starting_knife_inventory`
  - `GameplayServiceTests.test_merchant_can_sell_item_to_supply_for_full_acquisition_value`
  - `GameplayHtmlContextTests.test_dead_player_context_uses_ghost_view_projection`
  - `GameplayServiceTests.test_don_can_arm_silence_and_auto_apply_on_next_reported_murder`
  - `GameplayHtmlContextTests.test_detail_context_includes_actionable_superpower_panel_for_don`
  - `GameplayServiceTests.test_report_death_moves_session_to_accused_selection_when_police_alive`

## Session Update 2026-03-25 (Server Static Deployment Fix)
- GitHub and server were both confirmed on commit `0f25ff1` with `central-supply-item-icons/` present.
- Root cause of missing icons was nginx static path mismatch, not missing files:
  - nginx served `/static/` from `/var/www/mobboss/static/`,
  - Django `collectstatic` output existed under `/root/mob-boss-code-main/project/mobboss_apps/staticfiles/`.
- Verified fix:
  - sync collected static files into nginx alias target,
  - restart nginx,
  - verify `curl -I http://127.0.0.1/static/items/defaults/default_knife.jpg` and `...default_bulletproof_vest.png` return `200 OK`.
- Recommended deploy sequence on server after `collectstatic`:
  1. `SRC=/root/mob-boss-code-main/project/mobboss_apps/staticfiles`
  2. `DST=/var/www/mobboss/static`
  3. `mkdir -p "$DST"`
  4. `rsync -av --delete "$SRC"/ "$DST"/`
  5. `systemctl restart nginx`
- Important operator note:
  - avoid wrapped multi-line shell commands in terminal copy/paste; wrapped lines previously caused rsync destination/path errors and false 404s.

## Session Update 2026-03-25 (Droplet Theme + Deploy Workflow)
- Online white mobile footer/address-bar issue was not a missing `theme-color` commit in the main shell template.
- Verified:
  - droplet repo `HEAD` was already `0f25ff1`,
  - `project/mobboss_apps/web/templates/web/base.html` on the server already contained the dark `theme-color` meta tags.
- Root cause of persistent white mobile chrome:
  - unauthenticated requests were landing on `/auth/?next=/`,
  - `project/mobboss_apps/iam/templates/iam/auth.html` did not include the same `theme-color` meta tags as the main app shell.
- Additional deploy issue discovered:
  - droplet was serving an older webpack CSS bundle than local,
  - built frontend assets and `project/mobboss_apps/webpack-stats.json` are gitignored, so `git pull` alone does not update the active CSS/JS bundle on the server.
- Fixes applied:
  - added dark `theme-color` meta tags to `project/mobboss_apps/iam/templates/iam/auth.html`,
  - rebuilt frontend assets on the droplet with `npm run build`,
  - re-synced Django collected static files into nginx static root,
  - restarted `mobboss` and `nginx`,
  - confirmed the site loaded with correct styling and the footer/address chrome matched the page.
- Important deployment lessons:
  - use `/root/mob-boss-code-main/venv/bin/python3` for Django management commands on the droplet,
  - keep `collectstatic --noinput` on one line; splitting `--noinput` onto a new line cancels the command,
  - a transient `502 Bad Gateway` occurred during restart while gunicorn was between stop/start states; this resolved once `mobboss` came back healthy.
- Current one-line deploy command from local terminal:
  - `ssh root@134.199.226.15 'cd /root/mob-boss-code-main && git pull --ff-only origin main && npm run build && /root/mob-boss-code-main/venv/bin/python3 project/mobboss_apps/manage.py collectstatic --noinput && mkdir -p /var/www/mobboss/static && rsync -av --delete /root/mob-boss-code-main/project/mobboss_apps/staticfiles/ /var/www/mobboss/static/ && systemctl restart mobboss && systemctl restart nginx && curl -I http://127.0.0.1:8000/ && curl -I http://134.199.226.15/'`

## Session Update 2026-03-26 (Gameplay Rules + Economy Access + Jury Fairness + Detective Readability)
- Trial timing rules updated:
  - removed accused-selection timeout dependency from active gameplay flow (no auto/manual timeout progression required for normal play),
  - default jury voting no longer starts with a countdown timer,
  - Kingpin power changed to start a 15-second jury timer on the current trial (up to 2 uses on separate trials),
  - if Kingpin timer expires and zero jurors submitted a jury vote, trial resolves as accused set free and private notifications are sent to jurors, moderator, and Kingpin.
- Player economy access expanded:
  - all alive players (not only merchant faction) can now set resale prices and send player-to-player sale offers for items in their inventory,
  - merchant-only central-supply buy restriction remains in force,
  - Made Man exception for direct central-supply purchase remains in force.
- Merchant win-goal stability fixed:
  - merchant money goal is now fixed from session baseline (ledger circulating baseline) and no longer changes as players buy/sell/gift,
  - merchant win condition still triggers immediately when a merchant player's balance reaches or exceeds their fixed goal.
- Supplier Acquire player-facing notification copy updated:
  - seller/target message now reads:
    - `Acquire power was used. $X from your transaction was redirected to another player.`
  - recipient identity is no longer disclosed in that player-facing line.
- Jury selection randomness/fairness improved:
  - selection is now shuffled deterministically per trial using hash-based ordering,
  - when possible, players from the most recent jury are deprioritized so the next jury rotates participants better,
  - existing jury size constraints (fraction/min/odd) are preserved.
- Detective investigation readability fix applied:
  - detective reveal panel/table switched to light backgrounds with dark text for high contrast,
  - key CSS is in `project/mobboss_apps/web/webpack/styles.css` (`#detectiveInvestigationReveal` and `.detective-investigation-table*`).
- Detective visual mock asset generated for quick preview/reference:
  - `tmp/detective-table-mock.svg`

## Validation Snapshot (2026-03-26)
- `python3 -m unittest project.mobboss_apps.gameplay.tests.domain.test_game_service` passing.
- `python3 -m unittest project.mobboss_apps.gameplay.tests.adapters.test_page_view_mapper` passing.
- `python3 project/mobboss_apps/manage.py test project.mobboss_apps.gameplay.tests.rest.test_gameplay_v1_views` passing.
- `python3 project/mobboss_apps/manage.py test project.mobboss_apps.web.tests.rest.test_web_lobby` passing.
- `python3 project/mobboss_apps/manage.py check` passing.

## Updated Pickup Point
- If resuming gameplay rule work, begin in:
  - `project/mobboss_apps/gameplay/src/game_service.py`
  - `project/mobboss_apps/gameplay/views.py`
  - `project/mobboss_apps/gameplay/templates/gameplay/_jury_voting_card.html`
  - `project/mobboss_apps/gameplay/templates/gameplay/_player_wallet_card.html`
- If resuming detective readability/UI work, begin in:
  - `project/mobboss_apps/web/webpack/styles.css`
  - `project/mobboss_apps/gameplay/templates/gameplay/_superpower_card.html`
  - optional mock/reference: `tmp/detective-table-mock.svg`
- Recommended immediate follow-up playtest focus:
  1. Kingpin 15-second timer flow (partial votes vs zero-vote timeout branch).
  2. Non-merchant sale flow (`Set Price` -> `Sell Item` -> buyer accept/decline).
  3. Merchant fixed-goal behavior under repeated buy/sell/gift transactions.
  4. Jury rotation behavior over consecutive trials in the same session.

## Session Update 2026-03-27 (Phone-First Player UX + Offer Routing + Dev Tabs)
- Regular non-merchant, non-moderator, non-ghost players now use a phone-first screen-based layout in `project/mobboss_apps/gameplay/templates/gameplay/detail.html`:
  - home screen with 6 buttons in a 2x3 grid: `Game`, `Character`, `Wallet`, `Inventory`, `Note Pad`, `Murdered`
  - each destination screen has a blue `Home` button and avoids the old long scrolling stack
  - moderator, ghost, and merchant layouts intentionally remain on the old composition
- Character/home polish completed:
  - home header now shows logo on the left and `HOME SCREEN` / `Make a selection` on the right
  - logo asset copied into Django static at `project/mobboss_apps/static/branding/mobbosslogo.jpg`
  - player role card split into dedicated partial `project/mobboss_apps/gameplay/templates/gameplay/_player_role_card.html`
  - inventory split out of wallet into dedicated partial `project/mobboss_apps/gameplay/templates/gameplay/_player_inventory_card.html`
- Notification / overlay behavior updated:
  - generic notification overlay text/title now center correctly in the full-screen panel
  - multiple unseen notifications now advance one-by-one after `Okay`, newest first
  - jury submitted state now shows `Okay` and dismisses the jury overlay
  - notices seen during blocking accusation/jury overlays are marked seen so they do not replay later
- Offer handling for regular players changed:
  - incoming sale offers no longer rely on a popup; the receiver is routed directly to Wallet and scrolled to the purchase-request section
  - incoming item gifts likewise route the receiver directly to Wallet and scroll to the gift-offer section
  - wallet anchors added in `project/mobboss_apps/gameplay/templates/gameplay/_player_wallet_card.html`
  - sale-offer / gift-offer phone popup experiments were removed in favor of this wallet-routing approach because the wallet controls were the stable path
- Notification privacy fix:
  - declined sale offers, item gifts, and money gifts no longer broadcast to all players
  - decline notices now go only to sender, receiver, and moderator through `notification_feed`
  - changed in `project/mobboss_apps/gameplay/src/game_service.py`
- Economy pricing rule updated:
  - players and merchant-faction players can now set item resale prices to any non-negative dollar amount
  - backend no longer rounds resale prices to the nearest `$10`
  - price inputs now allow `$1` increments
- Moderator dev tabs update:
  - dev view tabs now show each participant's `role_name` instead of username once in game
  - changed in `project/mobboss_apps/gameplay/views.py`

## Validation Snapshot (2026-03-27)
- `python3 -m unittest project.mobboss_apps.gameplay.tests.domain.test_game_service` passing after pricing + decline-notification changes.
- `python3 project/mobboss_apps/manage.py check` passing repeatedly after template / phone-layout updates.

## Updated Pickup Point (2026-03-27)
- If resuming phone player UX work, begin in:
  - `project/mobboss_apps/gameplay/templates/gameplay/detail.html`
  - `project/mobboss_apps/gameplay/templates/gameplay/_player_wallet_card.html`
  - `project/mobboss_apps/gameplay/templates/gameplay/_player_inventory_card.html`
  - `project/mobboss_apps/gameplay/templates/gameplay/_player_role_card.html`
- If resuming offer / economy rules work, begin in:
  - `project/mobboss_apps/gameplay/src/game_service.py`
  - `project/mobboss_apps/gameplay/views.py`
  - `project/mobboss_apps/gameplay/tests/domain/test_game_service.py`
- Recommended immediate follow-up playtest focus:
  1. Regular-player phone flow when multiple notifications arrive back-to-back.
  2. Incoming sale offer auto-route to Wallet and direct accept/decline from wallet controls.
  3. Incoming item gift auto-route to Wallet and direct accept/decline from wallet controls.
  4. Arbitrary resale pricing, especially below acquisition cost, for both merchant and non-merchant sellers.
  5. Moderator dev-tab readability now that tabs show roles instead of usernames.
