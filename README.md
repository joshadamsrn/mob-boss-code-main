# mob-boss-code

## Web UI Notes

- The HTML surface is mobile-first and Bootstrap-based.
- Shared layout lives in `project/mobboss_apps/web/templates/web/base.html`.
- The protected main page (lobby) is at `/` and requires login.
- Authentication is available at `/auth/` with combined login and registration.

_
